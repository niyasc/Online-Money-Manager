<?php

    // configuration
    require("../includes/config.php"); 

    $news = query("select id, title from news limit 0,10 order by day desc");
    render("home.php",["title"=>"Home Page", "news" => $news]);

?>
